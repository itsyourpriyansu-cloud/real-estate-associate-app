import { LoginScreen } from '@/features/auth/LoginScreen';

/** 2 · ASSOCIATE LOGIN — phone → OTP → dashboard. */
export default function AssociateLoginScreen() {
  return <LoginScreen as="associate" />;
}
